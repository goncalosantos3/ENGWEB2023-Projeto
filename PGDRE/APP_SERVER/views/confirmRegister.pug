extends layout

block content
    .w3-card-4
    header.w3-container.w3-blue
      h1 Confirmação de registo
    
    .w3-container 
      h2 Utilizador criado com sucesso!
      ul.w3-ul 
        li #[b Nome de utilizador: ] #{u.username}
        li #[b Nome: ] #{u.name}
        li #[b Email: ] #{u.email}
        li #[b Nível: ] #{u.level}

      

    footer.w3-container.w3-blue
      address Generated in PGDRE at #{d} | #[a(href="/home") Go back to home!]